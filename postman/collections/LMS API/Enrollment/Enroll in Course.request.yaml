$kind: http-request
url: "{{baseUrl}}/api/v1/enrollments"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "courseId": "{{courseId}}"
    }
order: 1000
