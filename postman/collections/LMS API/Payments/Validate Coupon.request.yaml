$kind: http-request
url: "{{baseUrl}}/api/v1/coupons/validate"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "code": "SAVE20",
      "courseId": "{{courseId}}"
    }
order: 8000
