$kind: http-request
url: "{{baseUrl}}/api/v1/courses/my-courses/:courseId"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  courseId: "{{courseId}}"
body:
  type: json
  content: "{}"
order: 7000
