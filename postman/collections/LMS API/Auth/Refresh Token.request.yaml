$kind: http-request
url: "{{baseUrl}}/api/v1/auth/refresh-token"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "refreshToken": "{{refreshToken}}"
    }
order: 4000
