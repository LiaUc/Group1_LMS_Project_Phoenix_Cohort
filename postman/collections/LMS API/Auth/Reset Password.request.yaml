$kind: http-request
url: "{{baseUrl}}/api/v1/auth/reset-password/:token"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  token: "{{resetToken}}"
body:
  type: json
  content: |-
    {
      "newPassword": "NewPassword1"
    }
order: 6000
