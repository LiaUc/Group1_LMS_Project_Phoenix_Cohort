$kind: http-request
url: "{{baseUrl}}/api/v1/instructor/sections/:sectionId/lectures/reorder"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  sectionId: "{{sectionId}}"
body:
  type: json
  content: "{}"
order: 10000
