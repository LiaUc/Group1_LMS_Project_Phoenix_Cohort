$kind: http-request
url: "{{baseUrl}}/api/v1/instructor/sections/:sectionId/lectures/:lectureId"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  sectionId: "{{sectionId}}"
  lectureId: "{{lectureId}}"
body:
  type: json
  content: "{}"
order: 8000
