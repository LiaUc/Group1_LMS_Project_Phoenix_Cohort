$kind: http-request
url: "{{baseUrl}}/api/v1/questions/:questionId/answers/:answerId"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  questionId: "{{questionId}}"
  answerId: "{{answerId}}"
body:
  type: json
  content: "{}"
order: 6000
