$kind: http-request
url: "{{baseUrl}}/api/v1/personalization/learning-paths/:pathId/progress"
method: PATCH
headers:
  Content-Type: application/json
pathVariables:
  pathId: "{{pathId}}"
body:
  type: json
  content: "{}"
order: 12000
