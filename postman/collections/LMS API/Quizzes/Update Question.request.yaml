$kind: http-request
url: "{{baseUrl}}/api/v1/instructor/quizzes/:quizId/questions/:questionId"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  quizId: "{{quizId}}"
  questionId: "{{questionId}}"
body:
  type: json
  content: "{}"
order: 4000
