$kind: http-request
url: "{{baseUrl}}/api/v1/instructor/quizzes/:quizId"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  quizId: "{{quizId}}"
body:
  type: json
  content: "{}"
order: 2000
