$kind: http-request
url: "{{baseUrl}}/api/v1/assignments/:assignmentId/submit"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  assignmentId: "{{assignmentId}}"
body:
  type: json
  content: "{}"
order: 10000
