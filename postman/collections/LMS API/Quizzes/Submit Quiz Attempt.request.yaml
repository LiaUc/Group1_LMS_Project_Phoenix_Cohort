$kind: http-request
url: "{{baseUrl}}/api/v1/quizzes/:quizId/attempt"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  quizId: "{{quizId}}"
body:
  type: json
  content: "{}"
order: 6000
