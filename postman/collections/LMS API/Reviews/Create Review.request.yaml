$kind: http-request
url: "{{baseUrl}}/api/v1/courses/:courseId/reviews"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  courseId: "{{courseId}}"
body:
  type: json
  content: |-
    {
      "rating": 5,
      "comment": "Great course!"
    }
order: 2000
